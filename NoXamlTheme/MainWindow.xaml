<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NoXamlTheme.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NoXamlTheme"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="NoXamlTheme">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="20">
        <StackPanel x:Name="RootPanel"
                    Spacing="16">

            <TextBlock x:Name="TitleText"
                       Text="Theme Demo (C# Applied)"
                       FontSize="28"
                       FontWeight="Bold"/>

            <TextBlock x:Name="DescText"
                       Text="These brushes are applied in C#, not in XAML."
                       FontSize="16"/>

            <Button x:Name="AccentButton"
                    Content="Accent Button"
                    Width="220" Height="50"
                    CornerRadius="8"
                    Click="AccentButton_Click"/>

            <Border x:Name="AdaptiveBorder"
                    Width="300"
                    Height="120"
                    CornerRadius="12"
                    Padding="10"
                    HorizontalAlignment="Left"
                    BorderThickness="1">
                <TextBlock x:Name="AdaptiveBorderText"
                           Text="Adaptive Border"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
            </Border>

            <ComboBox x:Name="ThemeComboBox"
                      Width="200"
                      Header="Select Theme"
                      SelectionChanged="ThemeComboBox_SelectionChanged">
                <ComboBoxItem Content="System Default"
                              Tag="Default"
                              IsSelected="True"/>
                <ComboBoxItem Content="Light"
                              Tag="Light"/>
                <ComboBoxItem Content="Dark"
                              Tag="Dark"/>
            </ComboBox>
        </StackPanel>
    </Grid>
</Window>
